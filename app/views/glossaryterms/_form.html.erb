<%= form_for(@glossaryterm) do |f| %>
  <% if @glossaryterm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@glossaryterm.errors.count, "error") %> prohibited this glossaryterm from being saved:</h2>

      <ul>
      <% @glossaryterm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :term %><br>
    <%= f.text_field :term %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
